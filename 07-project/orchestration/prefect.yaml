# Welcome to your prefect.yaml file! You can you this file for storing and managing
# configuration for deploying your flows. We recommend committing this file to source
# control along with your flow code.

# Generic metadata about this project
name: orchestration
prefect-version: 2.11.1

# build section allows you to manage and build docker images
build: null

# push section allows you to manage if and how this project is uploaded to remote locations
push: null

# pull section allows you to provide instructions for cloning this project in remote locations
pull:
- prefect.deployments.steps.set_working_directory:
    directory: /home/ubuntu/notebooks/07-project/orchestration

# the deployments section allows you to provide configuration for deploying flows
deployments:
- name: mlops-project-orch
  entrypoint: orchestrate.py:main_flow
  work_pool:
    name: mlopsprojectpool
- name: mlops-project-orch-s3
  entrypoint: orchestrate_s3.py:main_flow_s3
  work_pool:
    name: mlopsprojectpool